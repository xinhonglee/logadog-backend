const roles = ['customer', 'manager', 'admin']
const genders = ['male', 'female', 'other']
const userStates = ['available', 'unavailable']
const dogStates = ['available', 'unavailable']

module.exports = {
  genders,
  roles,
  userStates,
  dogStates
}
